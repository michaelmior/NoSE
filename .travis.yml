language: ruby
cache: bundler
before_install: gem install bundler
bundler_args: --without=development gurobi
env: JRUBY_OPTS=--2.0
rvm:
  - 2.2.0
  - 2.1.5
  - 2.0.0
  - jruby-head
script: bundle exec rspec --tag ~gurobi
