# nose-<%= command.name %>(1) -- <%= command.description %>

## SYNOPSIS

`nose <%= command.usage %>` <%= command.options.each_value.map do |option|
  '[' + (option.aliases + ["--#{option.name}"]).join('|') + ']'
end.join ', ' %>

## DESCRIPTION

<%= command.long_description %>

## OPTIONS

<% command.options.each do |name, option| %>
  * <%= option.usage %>
  <%= option.description  %>

<% end %>
